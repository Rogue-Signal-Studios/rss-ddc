#ifndef RSS_DDC_ENUMERATION_H
#define RSS_DDC_ENUMERATION_H

#include <stddef.h>

/* Portable sizing rule shared by the public API implementation and tests. */
size_t rss_ddc_enumeration_write_count(size_t total, size_t capacity);

#endif
